export const metadata = {
    title: "Courses",
  };
  
  export default function chatLayout({ children }) {
    return (
      <div>
        <div className=""></div>
        <div className="flex items-center justify-center bg-gradient-to-tl from-[#ebacfb] to-[#f9fbc6] dark:bg-gradient-to-tl dark:from-[#b4b0b0] dark:to-[#504e4e]">{children}</div>
      </div>
    );
  }
  